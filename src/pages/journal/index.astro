---
import Layout from '../../layouts/Layout.astro'
import { getAllPosts } from '../../lib/sanity'
import JournalPostList from '../../components/content/journal/JournalPostList.astro'
import SimpleHero from '../../components/content/featured/SimpleHero.astro'

const posts = await getAllPosts()

// SEO optimized content
const pageTitle = 'Writing | Design & Development | Daniel Shires'
const pageDescription =
  'Read insights on UX/UI design, product development, and digital experiences. Articles on design thinking, user research, and creating impactful digital products.'
const pageImage = '/images/jpg/background.webp'

// Structured data for blog
const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'Blog',
  name: 'Daniel Shires — Writing',
  description: pageDescription,
  url: 'https://danielshires.com/journal',
  image: new URL(pageImage, 'https://danielshires.com').href,
  author: {
    '@type': 'Person',
    name: 'Daniel Shires',
    jobTitle: 'Product Designer',
  },
  publisher: {
    '@type': 'Person',
    name: 'Daniel Shires',
  },
  blogPost: posts.slice(0, 10).map((post) => ({
    '@type': 'BlogPosting',
    headline: post.title,
    description: post.description,
    author: {
      '@type': 'Person',
      name: 'Daniel Shires',
    },
    datePublished: post.publishedAt,
    dateModified: post.publishedAt,
    url: `https://danielshires.com/journal/${post.slug.current}`,
    keywords: post.tags?.join(', ') || 'design, development, UX, UI',
  })),
}
---

<Layout
  pageTitle={pageTitle}
  pageDescription={pageDescription}
  pageImage={pageImage}
  jsonLd={structuredData}
  pageType='website'>
    <!-- Hero Section -->
    <SimpleHero
      introText={{
        first: 'Writing',
        second: 'Notes, stories, and reflections on design, development, and the digital world.',
      }}
    />

    <section class='pb-16'>
      <JournalPostList posts={posts} />
    </section>
</Layout>
