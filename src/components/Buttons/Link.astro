---
const pathname = new URL(Astro.request.url).pathname;
const currentPath = pathname.slice(1);

interface Props {
	path: string;
	target: string;
	href: string;
}

const {href, target } = Astro.props;

---

<a class={''} href={href} target={target}><slot/></a>


