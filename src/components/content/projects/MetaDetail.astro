---
import Text from '../../ui/text/Text.astro'

interface Props {
  label: string
}

const { label } = Astro.props
---

<div class='flex flex-row gap-2 items-baseline'>
  <Text variant='overline' color='muted'>{label}</Text> /
  <Text variant='body' color='default'><slot /></Text>
</div>
