import backgroundImage from '../../public/images/jpg/background.webp'
import type { ImageMetadata } from 'astro'

export const images: { background: ImageMetadata } = {
  background: backgroundImage,
}
